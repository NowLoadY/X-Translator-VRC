[package]
name = "xrtranslate-prompt"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Serializable prompt graphs and deterministic translation prompt rendering."

[dependencies]
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"

[dev-dependencies]
